{
  "name": "pitchmonitor",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json && tsc -p app/worklet/tsconfig.json",
    "test": "vitest run",
    "predeploy": "yarn run build --base=/pitchmonitor/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "eslint": "^8.20.0",
    "gh-pages": "^6.3.0",
    "jsdom": "^29.1.1",
    "prettier": "^2.7.1",
    "typescript": "^6.0.3",
    "vite": "^3.0.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "pitchfinder": "^2.3.2"
  }
}
